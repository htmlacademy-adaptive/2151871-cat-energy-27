const slider=document.querySelector(".cat-comparison"),curtain=slider.querySelector(".range-bar");let curtainPlaceStart,clientX,sliderStyles=getComputedStyle(slider);function handleGrabCurtain(e){curtainPlaceStart=+sliderStyles.getPropertyValue("--curtain-place"),clientX=e.clientX,window.addEventListener("pointermove",handleMoveCurtain)}function handleMoveCurtain(e){let t=e.clientX-clientX,n=curtainPlaceStart+t/slider.clientWidth,r=Math.min(Math.max(n,0),1);slider.style.setProperty("--curtain-place",r)}function handleReleaseCurtaine(e){window.removeEventListener("pointermove",handleMoveCurtain)}window.addEventListener("pointerup",handleReleaseCurtaine),curtain.addEventListener("pointerdown",handleGrabCurtain);